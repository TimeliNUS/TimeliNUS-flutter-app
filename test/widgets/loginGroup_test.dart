import 'package:TimeliNUS/screens/landingScreen.dart';
import 'package:TimeliNUS/widgets/landingScreen/loginGroup.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import '../utils/widgetTest_util.dart';

void main() {
  // testWidgets('toggling checkbox changes state', (WidgetTester tester) async {
  //   final testWidget =
  //       makeTesteableWidget(child: LoginGroup(AuthenticationAction.login));
  //   await tester.pumpWidget(testWidget);
  //   await tester.pumpAndSettle();
  //   LoginGroupState loginGroupState = tester.state(find.byType(LoginGroup));
  //   expect(loginGroupState.isRemembered, false);
  //   await tester.tap(find.byType(Checkbox));
  //   expect(loginGroupState.isRemembered, true);
  // });

  // testWidgets('clicking "Remember me" changes state',
  //     (WidgetTester tester) async {
  //   // expect(true, true);
  //   final testWidget =
  //       makeTesteableWidget(child: LoginGroup(AuthenticationAction.login));
  //   await tester.pumpWidget(testWidget);
  //   await tester.pumpAndSettle();
  //   LoginGroupState loginGroupState = tester.state(find.byType(LoginGroup));
  //   expect(loginGroupState.isRemembered, false);
  //   final test = tester
  //       .element(find.text("Remember me"))
  //       .findAncestorWidgetOfExactType<InkWell>();
  //   await tester.tap(find.byWidget(test));
  //   expect(loginGroupState.isRemembered, true);
  // });
}
